node_name = "leaf6"
node_type = "frr"

[links.eth1]
neigh = "spine3"
iface = "eth3"

[links.eth2]
neigh = "spine4"
iface = "eth3"

[linux]
net = """
ip route del default

# Loopbacks
ip link add dev loopback0 type dummy
ip address add 10.0.0.6/32 dev loopback0
ip link set dev loopback0 up

# Underlay - p2p links to spines
ip address add 10.2.3.5/31 dev eth1
ip address add 10.2.4.5/31 dev eth2

# Bridge
ip link add br0 type bridge vlan_filtering 1 vlan_default_pvid 0
ip link add vxlan0 type vxlan dstport 4789 local 10.0.0.6 nolearning external vnifilter
ip link set vxlan0 master br0
ip link set br0 up
ip link set vxlan0 up
bridge link set dev vxlan0 vlan_tunnel on

# vrf for l3vni 10000
ip link add vrf10000 type vrf table 10000
ip link set vrf10000 up

# l3vni 10000 - vlan 1000
bridge vlan add dev br0 vid 1000 self
bridge vlan add dev vxlan0 vid 1000
bridge vni add dev vxlan0 vni 10000
bridge vlan add dev vxlan0 vid 1000 tunnel_info id 10000
ip link add vlan1100 link br0 type vlan id 1000
ip link set vlan1100 master vrf10000
ip link set vlan1100 up

# l2vni 160 - vlan 60
bridge vlan add dev br0 vid 60 self
bridge vlan add dev vxlan0 vid 60
bridge vni add dev vxlan0 vni 160
bridge vlan add dev vxlan0 vid 60 tunnel_info id 160
ip link add vlan60 link br0 type vlan id 60
ip link set vlan60 master vrf10000
ip addr add 192.168.60.254/24 dev vlan60
ip link set vlan60 up

# Client link
ip link set dev eth3 master br0
bridge vlan add vid 60 dev eth3 pvid 60 egress untagged
"""

[frr]
daemons = ["bgpd", "bfdd"]

net = """
route-map ALLOW permit 1
!
route-map RM_AS_RANGE permit 2
 match as-path PF_AS_RANGE
exit
!
vrf vrf10000
 vni 10000
exit-vrf
!
router bgp 65103
 bgp router-id 10.0.0.6
 no bgp ebgp-requires-policy
 neighbor SPINES peer-group
 neighbor SPINES remote-as 65100
 neighbor SPINES bfd
 neighbor SPINES password ebgp
 neighbor SPINES timers 1 3
 neighbor 10.2.3.4 peer-group SPINES
 neighbor 10.2.4.4 peer-group SPINES
 !
 address-family ipv4 unicast
  network 10.0.0.6/32
  redistribute connected
  neighbor SPINES route-map RM_AS_RANGE in
  neighbor SPINES route-map ALLOW out
  maximum-paths 4
 exit-address-family
 !
 address-family l2vpn evpn
  neighbor SPINES activate
  neighbor SPINES route-map RM_AS_RANGE in
  neighbor SPINES route-map ALLOW out
  advertise-all-vni
 exit-address-family
exit
!
router bgp 65103 vrf vrf10000
 !
 address-family ipv4 unicast
  redistribute connected
 exit-address-family
 !
 address-family ipv6 unicast
  redistribute connected
 exit-address-family
 !
 address-family l2vpn evpn
  advertise ipv4 unicast
  advertise ipv6 unicast
 exit-address-family
exit
!
bgp as-path access-list PF_AS_RANGE seq 5 permit 65100
!
end
"""
